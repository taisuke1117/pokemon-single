/**
 * 評価関数の各要素の重み（係数）を一元管理する。
 * 検算(オラクル盤面)しやすいよう1箇所に集約する。
 *
 * 段階1の手による階層付け:
 *   終端(±10000) ≫ 勝ち筋 > 物量(1.0/体) > 対面の脅威(0.3〜0.5) > 引き先(0.15/体) ≈ 設置・状態異常
 * 「確定した戦果ほど重く、見込みほど軽く」。
 */
export const WEIGHTS = {
  /** 終端(勝敗)。桁を分けて確定勝敗を評価点と交換させない。 */
  terminalWin: 10000,
  /** 生存1体=1.0。HP割合は満タンで+1.0（合計2.0）。 */
  survivalPerMon: 1.0,
  survivalHpFull: 1.0,
  /** 勝ち筋(抜き)。終端未満だが他要素より一段上。 */
  wincon: 4.0,
  /** 対面の脅威。今場の2体の殴り合い（±0.3〜0.5相当、内部で先後補正）。 */
  facingThreat: 0.4,
  /** 安全な引き先1体あたり。2-3体で頭打ち。 */
  safeSwitchInPerMon: 0.15,
  safeSwitchInCap: 3,
  /** 設置物1体分の交代見込みダメージ係数。 */
  hazards: 0.6,
  /** 状態異常（控えの将来価値）。 */
  statusBench: 0.5,
  /** 今アクティブへの継続効果（混乱/やどりぎ/かなしばり/挑発/アンコール/バインド/はんどう等）。 */
  activeVolatile: 0.3,
  /** みらいよち/はめつのねがいの発動予約（確定ダメージの期待値、turnsRemainingで割引）。 */
  pendingAttack: 0.35,
} as const;
